# Copyright 2026 BitWise Media Group Ltd
# SPDX-License-Identifier: MIT
#
# The dex namespace and the dex-secrets sync live in the per-cloud
# dex-cloud companion component (aws/google), which this core's emitted
# Kustomization depends on -- so the namespace and credentials exist
# before the chart reconciles.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - input-provider.yaml
  - resourceset.yaml
