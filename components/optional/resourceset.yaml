# Copyright 2026 BitWise Media Group Ltd
# SPDX-License-Identifier: MIT

# Component election: the optional tier (dex, flux-web, patchy) deploys
# only when named in the STACK_COMPONENTS cluster var — short names,
# comma-separated. Unset elects everything, so existing clusters are
# unaffected. kustomize can't conditionally include resources, so the
# per-component Flux Kustomizations are emitted from this template; the
# core tier (kyverno, cert-manager, external-dns, gateway, rbac) stays in
# stack/ unconditionally.
#
# The same var reaches inside components for cross-component wiring: dex
# only syncs / registers OAuth2 clients for elected relying parties, and
# patchy's status page drops to rollups-only (no SSO) on dex-less clusters.
#
# CLOUD gates the secret-sync machinery: GKE ships Integrated Secret
# Synchronization with the cluster, but on EKS the SecretSync CRD and its
# controller are the secret-sync component -- emitted only on aws, and only
# when a component that syncs secrets is elected. The consuming
# Kustomizations depend on it there, so the CRD exists before any
# ResourceSet renders a SecretSync against it.
apiVersion: fluxcd.controlplane.io/v1
kind: ResourceSet
metadata:
  name: optional
  namespace: flux-system
spec:
  inputs:
    - cloud: "${CLOUD:=google}"
      components: "${STACK_COMPONENTS:=dex,flux-web,patchy}"
  resourcesTemplate: |
    << $aws := eq inputs.cloud "aws" >>
    << $elected := splitList "," inputs.components >>
    << if and $aws (or (has "dex" $elected) (has "patchy" $elected)) >>
    ---
    apiVersion: kustomize.toolkit.fluxcd.io/v1
    kind: Kustomization
    metadata:
      name: secret-sync
      namespace: flux-system
    spec:
      interval: 30m
      retryInterval: 2m
      timeout: 5m
      wait: true
      prune: true
      # image verification must be live before the controller's pod admits
      dependsOn:
        - name: kyverno-policies
      sourceRef:
        kind: OCIRepository
        name: flux-system
      path: ./components/secret-sync
      postBuild:
        substituteFrom:
          - kind: ConfigMap
            name: cluster-vars
    << end >>
    << if has "dex" $elected >>
    ---
    apiVersion: kustomize.toolkit.fluxcd.io/v1
    kind: Kustomization
    metadata:
      name: dex
      namespace: flux-system
    spec:
      interval: 30m
      retryInterval: 2m
      timeout: 5m
      wait: true
      prune: true
      dependsOn:
        - name: kyverno-policies
        - name: gateway
        << if $aws >>
        - name: secret-sync
        << end >>
      sourceRef:
        kind: OCIRepository
        name: flux-system
      path: ./components/dex
      postBuild:
        substituteFrom:
          - kind: ConfigMap
            name: cluster-vars
    << end >>
    << if and (has "flux-web" $elected) (has "dex" $elected) >>
    ---
    # flux-web is the flux UI's SSO wiring and edge; without dex there is
    # nothing to configure, hence the and-guard.
    apiVersion: kustomize.toolkit.fluxcd.io/v1
    kind: Kustomization
    metadata:
      name: flux-web
      namespace: flux-system
    spec:
      interval: 30m
      retryInterval: 2m
      timeout: 5m
      wait: true
      prune: true
      dependsOn:
        - name: gateway
        << if $aws >>
        - name: secret-sync
        << end >>
      sourceRef:
        kind: OCIRepository
        name: flux-system
      path: ./components/flux-web
      postBuild:
        substituteFrom:
          - kind: ConfigMap
            name: cluster-vars
    << end >>
    << if has "patchy" $elected >>
    ---
    apiVersion: kustomize.toolkit.fluxcd.io/v1
    kind: Kustomization
    metadata:
      name: patchy
      namespace: flux-system
    spec:
      interval: 30m
      retryInterval: 2m
      # 10m rather than the workload default 5m: the first install also
      # waits on the SecretSyncs materialising the chart's Secrets; the
      # retry loop absorbs any remaining race.
      timeout: 10m
      wait: true
      prune: true
      # kyverno-policies: image verification (including the patchy
      # attestor) must be live before pods admit. gateway: the webhook
      # HTTPRoute attaches to the platform Gateway's https listener.
      dependsOn:
        - name: kyverno-policies
        - name: gateway
        << if $aws >>
        - name: secret-sync
        << end >>
      sourceRef:
        kind: OCIRepository
        name: flux-system
      path: ./components/patchy
      postBuild:
        substituteFrom:
          - kind: ConfigMap
            name: cluster-vars
    << end >>
