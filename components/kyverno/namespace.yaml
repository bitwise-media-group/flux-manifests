# Copyright 2026 BitWise Media Group Ltd
# SPDX-License-Identifier: MIT

# Kyverno's own namespace is excluded from its webhooks (chart default), so the
# policy engine can always restart itself even when the admission webhook is
# unavailable.
apiVersion: v1
kind: Namespace
metadata:
  name: kyverno
