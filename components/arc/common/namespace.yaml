# Copyright 2026 BitWise Media Group Ltd
# SPDX-License-Identifier: MIT

# The controller's namespace, named the way ARC's own documentation and
# the scale-set chart's controllerServiceAccount default expect. Scale
# sets are application-level and live in their own namespaces; only the
# controller and the listeners it launches run here.
apiVersion: v1
kind: Namespace
metadata:
  name: arc-systems
