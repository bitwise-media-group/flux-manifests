# Copyright 2026 BitWise Media Group Ltd
# SPDX-License-Identifier: MIT

# Everything elected, so every optional-tier Kustomization renders and gets
# kubeconform-validated. The aws render -- which additionally emits the
# secret-sync Kustomization and the dependencies on it -- comes from the
# resourceset.aws.yaml variant.
- cloud: google
  components: dex,flux-web,patchy
